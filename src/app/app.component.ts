import { Component } from '@angular/core'

@Component({
    // siehe das Tag <hs-root> in index.html !
    selector: 'hs-root',
    templateUrl: './app.component.html',
})
export class AppComponent {
    title = 'Kunde'
}
